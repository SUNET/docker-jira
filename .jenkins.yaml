docker_name: eduix/jira
builders:
  - script
script:
  - "echo \"Dummy job for starting branch specific ones\""
git:
  branches:
    - master
    - stable
    - staging
  extensions:
    checkout_local_branch: true
clean_workspace: true
downstream:
  - docker-jira-master
  - docker-jira-staging
  - docker-jira-stable

extra_jobs:
    - name: docker-jira-daily
      builders:
        - docker
      git:
        branch: master
      triggers:
        github_push: false
        cron: "@daily"
    - name: docker-jira-master
      builders:
        - docker
      docker_tags:
        - "master"
      git:
        branch: master
    - name: docker-jira-staging
      builders:
        - docker
      docker_tags:
        - "staging"
      git:
        branch: staging
    - name: docker-jira-stable
      builders:
        - docker
      docker_tags:
        - "stable"
      git:
        branch: stable
